# Non Real Assistant

مساعد شخصي ذكي محلي مع تسجيل دخول آمن عبر OTP على تيليجرام

## المميزات الحالية

- ✅ تسجيل دخول آمن عن طريق OTP
- ✅ إرسال رمز التحقق عبر Telegram
- ✅ واجهة عربية/إنجليزية
- ✅ قاعدة بيانات SQLite
- ✅ جلسات آمنة

## المتطلبات

- Python 3.8+
- Telegram Bot Token

## التثبيت

### 1. تثبيت المكتبات

```bash
cd non_real_assistant
pip install -r requirements.txt
```

### 2. إعداد Telegram Bot

1. افتح [@BotFather](https://t.me/botfather) في تيليجرام
2. أرسل `/newbot`
3. اتبع التعليمات لإنشاء البوت
4. احفظ الـ Token

### 3. إعداد ملف البيئة

```bash
cp .env.example .env
```

ثم عدّل ملف `.env`:

```env
SECRET_KEY=your-random-secret-key-here
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
```

### 4. التحقق من إعدادات البوت

```bash
python bot_info.py
```

هذا الأمر سيعرض لك:
- معلومات البوت
- رابط البوت
- تعليمات الإعداد

### 5. بدء محادثة مع البوت ⚠️ مهم جداً

**يجب عليك بدء محادثة مع البوت قبل تسجيل الدخول!**

1. افتح الرابط الذي ظهر من الأمر السابق (مثل: https://t.me/your_bot_username)
2. اضغط على "Start" أو أرسل أي رسالة للبوت
3. هذا يسمح للبوت بإرسال رسائل لك

### 6. معرفة Telegram ID الخاص بك

1. أرسل رسالة لـ [@userinfobot](https://t.me/userinfobot)
2. سيعطيك الـ ID الخاص بك
3. احفظه للخطوة التالية

### 7. إنشاء مستخدم

```bash
python create_user.py create --phone 01234567890 --telegram_id YOUR_TELEGRAM_ID
```

### 8. تحديث قاعدة البيانات (للجداول الجديدة)

```bash
python migrate.py
```

### 9. إضافة مساعد المنبه كمثال (اختياري)

```bash
python seed_assistant.py
```

### 10. تشغيل التطبيق

```bash
python app.py
```

افتح المتصفح على: `http://localhost:5000`

## إدارة المستخدمين

### إنشاء مستخدم جديد

```bash
python create_user.py create --phone 01012345678 --telegram_id 123456789
```

### عرض جميع المستخدمين

```bash
python create_user.py list
```

### حذف مستخدم

```bash
# حذف بالهاتف
python create_user.py delete --phone 01012345678

# أو حذف بالـ ID
python create_user.py delete --id 1
```

## البنية

```
non_real_assistant/
├── app.py                  # التطبيق الرئيسي
├── config.py               # الإعدادات
├── models.py               # نماذج قاعدة البيانات
├── auth.py                 # خدمات المصادقة
├── telegram_bot.py         # إرسال رسائل عبر تيليجرام
├── script_executor.py      # تنفيذ سكريبتات المساعدين
├── create_user.py          # إدارة المستخدمين
├── migrate.py              # تحديث قاعدة البيانات
├── seed_assistant.py       # إضافة مساعد المنبه كمثال
├── bot_info.py             # معلومات البوت
├── requirements.txt        # المكتبات المطلوبة
├── .env                    # متغيرات البيئة (لا تُرفع على Git)
├── static/
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── main.js
└── templates/
    └── login.html
```

## قاعدة البيانات

### الجداول:
- **users** - المستخدمين
- **otps** - رموز التحقق
- **assistant_types** - أنواع المساعدين المتاحة
- **actions** - الإجراءات التي يمكن تنفيذها
- **assistants** - المساعدين المفعلين للمستخدمين
- **tasks** - المهام
- **action_executions** - سجل تنفيذ الإجراءات

## المراحل القادمة

### Phase 1 (مكتمل): ✅
- [x] نظام تسجيل الدخول
- [x] قاعدة بيانات المساعدين والمهام
- [x] تنفيذ السكريبتات
- [x] مثال المنبه

### Phase 2 (مكتمل): ✅
- [x] Dashboard الرئيسي
- [x] API Endpoints كاملة
- [x] إحصائيات وبيانات فورية
- [x] إدارة المهام (CRUD)
- [x] **Background Scheduler للتذكيرات**
- [x] **إشعارات Telegram تلقائية**

### Phase 2.5 (قريباً):
- [ ] صفحة المساعدين الكاملة
- [ ] صفحة المهام الكاملة
- [ ] محرر السكريبتات (Code Editor)
- [ ] صفحة سجل التنفيذ

### Phase 3 (مستقبلاً):
- [ ] تكامل مع AI Model محلي
- [ ] WebSocket للإشعارات الفورية
- [ ] Mobile app
- [ ] Multi-language support

## الأمان

- جميع OTP صالحة لمدة 5 دقائق فقط
- الأكواد المستخدمة يتم إلغاؤها تلقائياً
- الجلسات محمية بـ SECRET_KEY
- لا يتم تخزين كلمات المرور

## حل المشاكل الشائعة

### ❌ Chat not found

**المشكلة:** البوت لا يستطيع إرسال رسائل للمستخدم

**الحل:**
1. افتح تيليجرام وابحث عن اسم البوت (استخدم `python bot_info.py` لمعرفة اسم البوت)
2. اضغط على "Start" أو أرسل أي رسالة للبوت
3. حاول تسجيل الدخول مرة أخرى

### ❌ Bot is blocked by user

**المشكلة:** المستخدم حظر البوت سابقاً

**الحل:**
1. افتح المحادثة مع البوت في تيليجرام
2. اضغط على "Unblock" أو "إلغاء الحظر"
3. أرسل رسالة للبوت للتأكد من عمله
4. حاول تسجيل الدخول مرة أخرى

### ❌ Bots can't send messages to bots

**المشكلة:** استخدمت Telegram ID الخاص بالبوت بدلاً من ID الشخصي

**الحل:**
```bash
# 1. احصل على Telegram ID الشخصي من @userinfobot
# افتح تيليجرام وابحث عن: @userinfobot
# أرسل له أي رسالة وسيرد بـ ID الخاص بك

# 2. احذف المستخدم القديم
python create_user.py delete --phone 01234567890

# 3. أنشئ مستخدم جديد بالـ ID الصحيح
python create_user.py create --phone 01234567890 --telegram_id YOUR_PERSONAL_ID
```

**ملاحظة:** Telegram ID الشخصي عادة يكون 9-10 أرقام، بينما Bot ID يكون أطول (10-13 رقم)

### ❌ Invalid phone number

**المشكلة:** رقم الهاتف غير مسجل

**الحل:**
```bash
# تحقق من المستخدمين المسجلين
python create_user.py list

# إنشاء مستخدم جديد
python create_user.py create --phone 01234567890 --telegram_id YOUR_ID
```

### ❌ Event loop is closed

**المشكلة:** خطأ في async/await

**الحل:**
- تم إصلاح هذه المشكلة في النسخة الحالية
- تأكد من تحديث الملفات
- أعد تشغيل التطبيق

## الترخيص

هذا مشروع شخصي - استخدمه كما تشاء! 🚀

## المساعدة

للإبلاغ عن مشاكل أو اقتراحات، افتح Issue في GitHub